{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "api:generate": {
      "dependsOn": ["@cnet/api#api:generate"],
      "outputs": ["src/generated/**"]
    }
  }
}
